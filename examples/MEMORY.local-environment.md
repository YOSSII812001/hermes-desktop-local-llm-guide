The active Hermes Desktop profile is under %LOCALAPPDATA%\hermes on this Windows machine. Confirm the active config path before editing.
§
The user stores long notes and generated documents in an Obsidian vault. Never store credentials in the vault.
§
The local model uses an OpenAI-compatible endpoint. Verify availability with the endpoint health check instead of assuming it is running.
§
Hermes configuration or channel skill binding changes require a new session before verification.
